// Make a function that will return a greeting statement
// that uses an input; your program should return,
//   "Hello, <name> how are you doing today?".

function greet(name){
  return `Hello, ${name} how are you doing today?`
}
//to use template literals the use of backticks `` must be used 
//the traditional strings indicators of "" or '' 
//will NOT work for template literals

//single line solution
// const greet = name => `Hello, ${name} how are you doing today?`;

//encapsulate variables with ${expression}
