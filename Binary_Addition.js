function addBinary(a,b){
    return (a+b).toString(2) //toString(2) is a to string method that auto converts an integer to its binary value in string format
  }

  //shorthand solution
  const addBinary = (a, b) => (a + b).toString(2);